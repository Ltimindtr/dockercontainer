name: Trigger auto deployment for sntestimage

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/sntestimage-AutoDeployTrigger-6bc6cd01-59ae-4fd9-858c-fa95c534247e.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.SNTESTIMAGE_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SNTESTIMAGE_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SNTESTIMAGE_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: acrsnreg.azurecr.io
          registryUsername: ${{ secrets.SNTESTIMAGE_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SNTESTIMAGE_REGISTRY_PASSWORD }}
          containerAppName: sntestimage
          resourceGroup: Snambissan
          imageToBuild: acrsnreg.azurecr.io/sntestimage:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


